[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "velith"
version = "0.0.0"
description = "Velith — a grounded, verification-first engineering intelligence (M0 skeleton)."
readme = "README.md"
requires-python = "==3.12.*"
license = { text = "Proprietary" }
authors = [{ name = "Velith" }]
dependencies = [
    "pydantic==2.9.2",
    "pydantic-settings==2.5.2",
]

[project.optional-dependencies]
dev = [
    "ruff==0.6.9",
    "mypy==1.11.2",
    "pytest==8.3.3",
    "pre-commit==3.8.0",
]
# M9 analysis stack (handoff OED-3/OED-6): isolated optional group, installed only in
# the verifier image + CI. Only statsmodels is pinned here; its exact transitive matrix
# (numpy/scipy/pandas/patsy/formulaic/packaging + transitives) is locked in
# constraints/analysis-py312.txt, resolved inside python:3.12.7-slim-bookworm (OED-5).
analysis = [
    "statsmodels==0.15.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/velith"]

# ----------------------------------------------------------------------------
# Ruff: single source of truth for lint AND format (M0 §7).
# ----------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["velith"]

# ----------------------------------------------------------------------------
# mypy: strict mode, zero errors required (M0 §7).
# ----------------------------------------------------------------------------
[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
mypy_path = "src"
explicit_package_bases = true
namespace_packages = true
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
disallow_any_generics = true
no_implicit_reexport = true

# Scoped override (M9 handoff OED-6): statsmodels ships no type stubs. Confined to
# statsmodels.* so `mypy --strict` stays global everywhere else; statsmodels is isolated
# behind the typed analysis/gee.py adapter (implemented in a later M9 commit, not now).
[[tool.mypy.overrides]]
module = ["statsmodels.*"]
ignore_missing_imports = true

[tool.pydantic-mypy]
init_forbid_extra = true
init_typed = true
warn_required_dynamic_aliases = true

# ----------------------------------------------------------------------------
# pytest: tests discovered under tests/ (M0 §8).
# ----------------------------------------------------------------------------
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
addopts = "-q"

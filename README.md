# Velith — M0 skeleton

Project skeleton and environment validation. M0 proves the toolchain runs
end-to-end — Docker build, containerized pytest, config load, structured
logging, and CI green from a fresh clone — before any agent, model, dataset,
or verification logic exists.

> Run command and prerequisites are finalized in commit 8 of M0.
